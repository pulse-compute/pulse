# D01: hosted documentation history inventory

Captured 2026-09-25 UTC from `latest` at `a0105747a937430141d7e91213544b0a11bac76d`. This is a read-only evidence ledger for D02/D03, not a deployment candidate or authorization to change an immutable object. Entry point: none (repository release evidence under the ordinary root and `wasm/` instruction chain).

## Evidence and limits

- The production [documentation deployment workflow](https://github.com/pulse-compute/pulse/blob/latest/.github/workflows/documentation-deploy.yml) has recorded dispatches [beta.1](https://github.com/pulse-compute/pulse/actions/runs/31137278781), [beta.2 first writer](https://github.com/pulse-compute/pulse/actions/runs/34783329517), [beta.2 retag](https://github.com/pulse-compute/pulse/actions/runs/34793794583), [beta.3](https://github.com/pulse-compute/pulse/actions/runs/34854729563), and [beta.5](https://github.com/pulse-compute/pulse/actions/runs/35414098440). No beta.4 dispatch appears in the workflow run history through this capture. Each cited run's head SHA is the checked-out tag commit. The downloadable production verification artifacts for beta.1 and the first beta.2 run, and the sealed candidate artifacts for both beta.2 runs, beta.3, and beta.5, supply the counts and hashes below. These artifacts have retention deadlines; the stable run IDs and critical hashes are recorded here.
- The production verifier reads each stored object's bytes and `head-object` content type and cache policy before declaring an immutable match; it compares the actual byte SHA-256, not only the stored `pulse-sha256` metadata. A terminal `status: ok` in the production verification artifact therefore proves matching bytes and those two HTTP metadata fields **at that run's time**. The artifact does not retain a fresh `head-object` response for today. No protected storage credential was accessed for this inventory.
- Live public fetch and browser checks on 2026-09-25 UTC found [the root homepage](https://pulsecompute.io/) displaying beta.1, [the beta.1 exact homepage](https://pulsecompute.io/v1.0.0-beta.1/), [the beta.2 exact homepage](https://pulsecompute.io/v1.0.0-beta.2/), and [the beta.2 changelog](https://pulsecompute.io/v1.0.0-beta.2/changelog/) renderable. The beta.2 changelog's “Browse latest” link resolved to the beta.1 changelog. The web search fetcher could not inspect `latest` or beta.3–beta.5 exact routes; this is **not** evidence of HTTP 404. Those uninspected routes and today's direct storage metadata remain unverified.

## Hosted-state ledger

| Version | Tagged source / production run | Storage and public state | Preservation decision |
| --- | --- | --- | --- |
| beta.1 | `969b8f43e87e4c2bce3b575cb702bc6dfd5c816b`; [run 31137278781](https://github.com/pulse-compute/pulse/actions/runs/31137278781) | **Publicly served.** 101 exact-version files plus receipt uploaded and storage-verified (102 immutable); 87 mutable objects promoted and verified after npm verification. That run failed only at public verification because `/latest/` did not converge to its expected body. Today's root and exact homepage show beta.1, and the beta.2 page's latest link lands at beta.1. | Preserve the 101 `https://pulsecompute.io/v1.0.0-beta.1/**` files and immutable receipt `pulse/deployments/v1.0.0-beta.1.json`. Root and `latest` are mutable aliases, currently beta.1, and can advance only through the gated release path. |
| beta.2 | First writer `16444cc5e116498e1fdf5f1e4815b4b8c3c74349`; [run 34783329517](https://github.com/pulse-compute/pulse/actions/runs/34783329517). Current tag `6bd40610fea7a633bb05e048714cbccc7a3ecb67`; [retag run 34793794583](https://github.com/pulse-compute/pulse/actions/runs/34793794583). | **Partly published:** first run uploaded 102 beta.2 exact files plus its receipt (103 new immutable objects) and verified them alongside 101 already-present beta.1 files (204 total); it stopped because `@pulse-compute/runtime@1.0.0-beta.2` was not yet on npm, before mutable promotion. The exact homepage and changelog are publicly served today. The retagged run stopped immediately at the already-present beta.2 receipt. | Preserve all 102 `https://pulsecompute.io/v1.0.0-beta.2/**` objects **with their first-uploaded bytes**, plus original receipt `pulse/deployments/v1.0.0-beta.2.json`. The current beta.2 source archive is not a safe replacement for its served changelog. Do not promote beta.2 to `latest`. |
| beta.3 | `46d838e1fab9b24e91b1dabe6b16f162e9392db4`; [run 34854729563](https://github.com/pulse-compute/pulse/actions/runs/34854729563) | **Never hosted by the recorded production dispatch.** Its immutable phase encountered the already-present beta.2 changelog conflict before any beta.3 key. No npm gate, mutable promotion, or public verification ran. Direct public route and present bucket absence were not proved. | No beta.3 hosted URL is established by this evidence. Retain the Git tag/source separately from the hosted preservation set. |
| beta.4 | `93142c6e9c6945f4e71b0ee1562da201581f1844`; no recorded documentation deployment dispatch | **No recorded host publication.** There is a checked-in beta.4 archive and Git tag, but no production object upload or public verification in the deployment run history. Direct public route and present bucket absence were not proved. | Do not infer that the checked-in archive or npm tag created hosted URLs. |
| beta.5 | `91cc3145e7ad4bec5d94c228d95016b0f77d04fd`; [run 35414098440](https://github.com/pulse-compute/pulse/actions/runs/35414098440) | **Never hosted by the recorded production dispatch.** Tag, build, seal, and protected configuration passed; immutable upload stopped at beta.2 changelog before any beta.5 key. No npm gate, mutable promotion, or public verification ran. Direct public route and present bucket absence were not proved. | The npm/Git tag remains source history. Beta.5 is not a docs recovery or promotion target. D03 prepares the next exact release after D02. |

The first beta.2 production deployment report says `uploaded: 103`, `alreadyPresent: 101`, `replaced: 0`, `deleted: 0`; its storage verification has `status: ok` for all 204 immutable keys. The beta.1 report says 102 immutable uploads and 87 mutable uploads, each verified with no delete. “Never hosted” above is scoped to **the recorded production workflow**; a separate manual storage writer cannot be ruled out without a new read-only bucket inventory. This distinction matters when deciding which public URLs must survive.

## First writer and exact conflict

| Immutable key | First successful storage verification (beta.2 source `16444cc5`) | Retagged beta.2 candidate (`6bd40610`) | Later beta.3/beta.5 candidates |
| --- | --- | --- | --- |
| `pulse/v1.0.0-beta.2/changelog/index.html` | 28,781 bytes; SHA-256 `3f908cd334671cad46e00375d30de099126b687b313e9db3b6a06a6944cf0688` | 28,808 bytes; SHA-256 `0e8371c7400409e179af7cbc77e8b3a03062fd100bd25251f9d4282a10a1e21f` | Same new SHA-256 `0e8371c7400409e179af7cbc77e8b3a03062fd100bd25251f9d4282a10a1e21f` |
| `pulse/deployments/v1.0.0-beta.2.json` | SHA-256 `83efb4ad9013ac36236b4693d86cb837337eb774e4885722025638850aae839a` | SHA-256 `c882d7eeb7a8a4ea0b73a90da6a87a3eb2d69d2deb762ecb592c85b83eed999b` | Historical receipt is not a later release's candidate object. |

The two beta.2 candidate manifests contain exactly 102 beta.2 files at the same paths. **Only the changelog's bytes differ**; all 102 entries have matching declared content types and cache policies. Its current checked-in archive has the new changelog SHA. The [source diff between the two tag commits](https://github.com/pulse-compute/pulse/compare/16444cc5e116498e1fdf5f1e4815b4b8c3c74349...6bd40610fea7a633bb05e048714cbccc7a3ecb67) changes `CHANGELOG.md` and its installed CLI copy: the K4 link changed from a tag-based GitHub source URL to an exact commit URL. The generated HTML consequently changed. The receipt's source commit and site inventory digest changed with the retagged candidate, so its bytes also differ. The current beta.2 tag resolves to `6bd40610…`, whereas the first successful storage writer checked out `16444cc5…` under the same tag name.

The first writer verified the old changelog bytes in Object Storage. Later candidates expect the new bytes, with **no declared content-type/cache-policy difference**. The beta.2 receipt conflict on retry and the changelog conflicts on beta.3/beta.5 are therefore explained by known byte differences; a metadata-only mismatch is unnecessary to explain any of these failures. The generic error message itself does not say which comparison failed. A fresh protected `head-object` plus content hash would be needed to re-attest the bucket's current state, but must remain a human-owned read-only infrastructure action; never overwrite either original object to make a candidate pass.

## Consequences for the next tasks

1. D02 must select only the **new current exact-version tree and its own receipt** for immutable production upload/verification. Existing historical keys should be neither rewritten nor replayed by a later release. The beta.1 and beta.2 public exact URLs above must continue to resolve to their stored bytes; the first beta.2 changelog and receipt hashes are the preservation anchors. The already-checked-in older trees may still support local preview/validation.
2. D03 should prepare a new reviewed exact release and audit package links to exact-version documentation. Do not repoint `latest` to beta.5 or treat an npm/Git tag as proof of hosted documentation. A release owner can perform a read-only production bucket inventory for the unverified beta.3–beta.5 keys and fresh hashes before making any stronger absence claim.
